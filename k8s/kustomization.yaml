apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  # - ingress.yaml  # Uncomment if you want to use ingress

commonLabels:
  app: fastapi-hello

